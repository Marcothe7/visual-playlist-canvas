# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Environment variables — never commit real secrets
.env
.env.local
.env.production
.env.*.local

.vercel
.env*.local

# Capacitor native projects (large generated folders — rebuilt by npx cap add)
# If you need to customize native files (e.g. AndroidManifest.xml for deep links),
# remove these entries and commit the specific files instead.
android/
ios/
